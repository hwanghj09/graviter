# 게임 한줄 설명

이 게임은 중력을 이용한 퍼즐 게임이다. 플레이어는 앞에 있는 물체를 가지고 올 수 있는 중력건과 중력을 바꿀 수 있는 장비를 장착한 상태이다. 이 장비들을 통해 각 스테이지별로 퍼즐을 풀어 목적지까지 나아가는 형식이다.

# 게임 규칙

스테이지 클리어 조건 : 목적지(포탈) 타일에 플레이어가 도달하면 해당 스테이지 클리어

중력 변환 규칙 : Z키를 눌러 시계 방향으로 플레이어의 중력이 변환된다. 단, 중력이 변화되어도 플레이어가 들고 있는 물체의 중력은 변화되지 않는다.

상호작용 규칙 : SHIFT키를 누르면 바라보고 있는 방향으로 일정한 거리(블럭 2칸) 안에 있는 블럭 1개를 잡고, 놓을 수 있음, 블럭을 들고 있는 상태에서는 플레이어의 속도가 20% 추가 감소함, 잡고있는 블럭은 플레이어의 이동을 막지 않음

웅크리기 규칙 : 플레이어의 히트박스도 같이 줄어듦 (1칸짜리 블럭도 이동 가능), 웅크리기 시 플레이어 속도 20% 감소
게임 규칙
- 플레이어가 스테이지를 클리어하면 자동으로 다음 스테이지로 이동함,
- 플레이어가 다른 페이지로 이동하거나 ESC를 눌렀을 때 일시정지 UI가 나타남

게임 저장 규칙
- 플레이어가 스테이지를 클리어할 때마다 클리어한 스테이지를 로컬에 저장함
- 조작키를 변경하면 변경된 조작키를 저장함

# 게임 상태

메인 화면
스테이지 선택
일시정지
인게임 플레이
스테이지 클리어
게임 오버/재시작
엔딩

# 스타일, 에셋

플레이어 : 졸라맨
타일 스프라이트 : 단일 검은색 블럭
상호작용 블럭 : 회색 블럭


# UI 뼈대

메인 화면 : `시작하기`, `설정` 버튼 표시, `시작하기` 버튼을 눌렀을 때, `스테이지 선택` UI로 이동

`설정` 버튼을 눌렀을 때 `설정` UI로 이동

- `스테이지 선택` : 만들어져있는 스테이지가 모두 표시됨, 클리어하지 않은 스테이지와 클리어된 스테이지, 열려있는 스테이지, 잠겨있는 스테이지가 각각 구분되게 디자인되어있음.

- `설정` : `조작키`, `사운드` 버튼 표시
- `조작키` : 각 상하좌우 이동의 조작키를 변경할 수 있는 버튼
- `사운드` : slider를 통해 소리 조절

`일시정지` : 인게임 플레이 중 ESC를 눌렀을 때 표시, 각 계속하기, 설정, 나가기 버튼 표시



# 조작 방법

(기본 설정)

`A`,`왼쪽화살표` : 왼쪽으로 이동

`D`,`오른쪽화살표` : 오른쪽으로 이동

`W`,`위쪽화살표` : 점프

`S`,`아래화살표` : 웅크리기

`Z` : 중력이 90도씩 회전함 (카메라도 같이 회전함)

`SPIFT` : 바라보고 있는 방향의 블럭을 잡음

# 최종 목표

퍼즐을 풀어 모든 스테이지를 클리어하는 것이 이 게임의 목표이다.

# 개발 스택
html, css, js로만 만들 것
js를 각 기능별로 나눠어서 파일을 만들어